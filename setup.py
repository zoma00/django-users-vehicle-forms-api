from setuptools import setup, find_packages

setup(
  name='Api-django-users-vehicle',
  version='3.12.0',
  packages=find_packages(),
  include_package_data=True,
  description='An API for managing users and their vehicles in Django',
  author='Hazem-Adel',
  author_email='zoma0097@gmail.com',
  license='MIT',
  # Add install_requires if you have specific dependencies
  # install_requires=['django>=5.2,<4.0']
)
